\documentclass[9pt]{extarticle}
  
  % All package imports, global styles & ugly tweaks go here
  % Not intended to be changed by the user, so keep them separate
  \input{admin/setup}
  \usepackage{lipsum}

  % For usage see the 'parti-courses.tex' file.
  % Main wrapper/containter for each module/course
  \newenvironment{studymodule}[3]
    {\textbf{\MakeUppercase{#1}}\hfill\textbf{#2 Lectures, Term #3}\par\vspace*{2mm}\small}
    {\normalsize\vfill\null\columnbreak}
  
  % Helpful macro to update the header text & display the main heading at the start of each 'part'
  \newcommand{\blockheading}[1]{\def\headertitle{#1}{\centering\huge\textbf{\MakeUppercase{#1}}\par}\vspace*{5mm}}

  % Sometimes a course has pre-requisites. This handles their format; not location.
  \newcommand{\prereq}[1]{\textit{#1}\par\vspace*{2mm}}

  % Each course covers a few different topics. This handles their format; not location.
  \newcommand{\studytopic}[3]{\textbf{#1}\\#3\hfill[#2]\par}

  % A short-cut command to display a default heading before 'recommended books' are listed.
  \newcommand{\booksection}{{\centering\textbf{Appropriate Books}\par}}

  % Often courses have some 'recommended books'. This handles their format; not location.
  % A general book
  \newcommand{\studybook}[3]{#1 \textit{#2}. #3\\}
  % An important/main book
  \newcommand{\studybookreq}[3]{\hspace*{-0.5em}\makebox[0.4em]{\raisebox{0.5em}[0.1em]{$\dagger$}}#1 \textit{#2}. #3\\}

\begin{document}
  % The cover page. Kept separate for clarity.
  \input{admin/cover}

  % A one-off introductory section for the whole document.
  \input{admin/intro}
  
  % Full material (including secondary introductions) for a given year/part.
  % Add as many (different ones) as needed.
  \input{parts/part1}
  \input{parts/part2}
\end{document}